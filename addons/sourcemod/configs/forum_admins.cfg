"forum_admins"
{
    "3" // GroupID, in my installation is the groupid 3 the root admin
    {
        // group will be preferred, if plugin can't find a group with this name, it'll use the flags and immunity values.
        // Uncomment and update the next line for using this.
        // "group"     "Any (already exist) Group Name"
        "flags"     "z"
        "immunity"  "100"
    }
}
